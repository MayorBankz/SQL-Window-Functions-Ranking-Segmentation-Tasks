# SQL-Window-Functions-Ranking-Segmentation-Tasks
This document covers practical use cases of: ROW_NUMBER(), RANK(), DENSE_RANK(), NTILE(), CUME_DIST(), PARTITION BY, ORDER BY inside window functions
